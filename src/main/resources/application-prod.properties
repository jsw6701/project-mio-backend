spring.datasource.url=jdbc:mysql://${HOST}:${PORT}/${NAME}?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
spring.datasource.username=${USER}
spring.datasource.password=${PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

google.client.id=${CLIENT}
# jwt
jwt.secret=${JWT}

discord.webhook.url=${DISCORD}

server.ssl.key-store= C:/project-mio-backend/src/main/resources/keystore.p12
server.ssl.key-store-password=${SSLPWD}
server.ssl.key-store-type=PKCS12